# Self-contained: standard scientific stack only (no `domain` / no product packages).
# Pinned to the environment the reported results were produced on (Python 3.12).
numpy==2.4.1
scipy==1.17.0
matplotlib==3.10.7
mne==1.11.0
